package org.dreamwork.integration.httpd.support;

public enum ParameterLocation {
    Auto, Body, ContextAttribute, Cookie, Form, Header, Internal, Path, RequestAttribute, SessionAttribute, QueryString
}
